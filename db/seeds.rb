Tag.create!(
  name: "Для офферов",
)

# group = Group.first
# group.tags << Tag.last
# # User.first.tags << Tag.last

# Group.for_offers.each do |group|
#   group.active!
# end

# Group.all.each do |group|
#   group.inactive!
# end
